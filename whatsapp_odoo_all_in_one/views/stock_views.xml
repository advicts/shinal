<odoo>
    <data>
        <record id="view_whatsapp_integration_stock_picking_form" model="ir.ui.view">
            <field name="name">stock.picking.whatsapp integration.form.inherit</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header/button[last()]" position="after">
                    <button name="send_whatsapp_msg"
                            string="Send by WhatsApp"
                            type="object"
                            class="oe_highlight"
                            icon="fa-whatsapp"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>